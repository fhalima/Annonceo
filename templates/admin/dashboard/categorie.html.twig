{% extends "admin/_template.html.twig" %}

{% block title %}
    Back Office
{% endblock %}

{% block content %}

<div class="section">
    <h2 class="title">
        Gestion des Catégories
    </h2>
    <p></p>

    <hr>
    <h3 class="subtitle">

    </h3>

    <div class="table-container is-scrollable">
        <table class="table is-bordered is-striped is-narrow is-hoverable is-fullwidth is-small ">
            <thead>
            <tr>
                <th>Id catégorie</th>
                <th>titre</th>
                <th>mots clés</th>
                <th>actions</th>

            </tr>
            </thead>

            <tbody>
            {% for categorie in categorieList %}
                <tr>
                    <th>{{ categorie.id }}</th>
                    <td>{{ categorie.titre }}</td>
                    <td>{{ categorie.motscles }}</td>
                    <td>
                        <small> <a href="#"> <i class="fas fa-search"></i>
                            </a></small>
                        <small> <a href="{{ path('admin_categorie_update',{id:categorie.id }) }}"> <i
                                        class="far fa-edit"></i> </a></small>
                        <small> <a href="{{ path('admin_categorie_delete',{id:categorie.id }) }}"> <i
                                        class="far fa-trash-alt"></i> </a></small>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <hr>
    {% include 'includes/flash_message.html.twig' %}
    <div class="form_container" id="form">

        {{ form_start(categorie_form) }}

        {{ form_row(categorie_form.titre , {label: 'Titre', value: categorie_curr.titre}) }}
        {{ form_row(categorie_form.motscles, {label: 'Description l  ongue', value: categorie_curr.motscles}) }}

        <button class="button is-primary is-center" type="submit" id="submit" name="submit">
            {{ categorie_button }}
        </button>

        {{ form_end(categorie_form) }}

    </div>
</div>
    {% endblock %}

